# Static assets - serve directly (no SPA fallback)
/js/*  /js/:splat  200
/css/*  /css/:splat  200
/images/*  /images/:splat  200
/assets/*  /assets/:splat  200
/public/*  /public/:splat  200
/guides/*  /guides/:splat  200

# API routes are handled by functions/api/[[catchall]].js
# (Do NOT add /api/* redirects here - they conflict with Pages Functions)

# SPA fallback - all other routes serve index.html
/*  /index.html  200
