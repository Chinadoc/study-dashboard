# Static assets - serve directly (no SPA fallback)
/js/*  /js/:splat  200
/css/*  /css/:splat  200
/images/*  /images/:splat  200
/assets/*  /assets/:splat  200
/public/*  /public/:splat  200

# Auth endpoints: redirect to Worker directly (browser navigates to Worker, which then redirects to Google)
/api/auth/*  https://euro-keys.jeremy-samuels17.workers.dev/api/auth/:splat  302

# API endpoints - Proxy to Worker
/api/*  https://euro-keys.jeremy-samuels17.workers.dev/api/:splat  200

# SPA fallback - all other routes serve index.html
/*  /index.html  200


/api/assets/*  /assets/:splat  200
